типы данных
int - целые числа
double - число с плавающей точкой
bool (true, false) - логические
string - строковый тип в двойных кавычках
char e = 'x' - символ

обьявлять епременные одного типа можно через запятую



dotnet new console - новый проект
dotnet run - запуск проекта
Write(); - вывод в одну строку

WriteLine(); - в конце строки перейти на новую строку
("my number is " + a) можно сложить строку с значением переменной
($"my numbers are {a} and {b} cool") - доллар и фигурные скобки.

ReadLine(); - считывает строку из терминала
int num = Convert.ToInt32(Console.ReadLine())

new Random().Next(min, max) - выдает случайное целое число 
от мин до мах

if (условие)
{
Набор действий 1
}
else 
{
Набор действий 2
}

While (условия продолжения)
{
Набор действий
}
int count = 0;
